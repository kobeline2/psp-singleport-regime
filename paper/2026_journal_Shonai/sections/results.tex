\section{Results}
\label{sec:results}

\subsection{Overview of experimental runs and data consistency}
\label{sec:results_overview}
% 役割は2つだけです.
% 	•	24 runs が予定通り取得できたこと
% 	•	静水開始条件や流量条件が概ね揃っていたこと
% ここでやることは最小限で十分です.
% 	•	実施できた run 数
% 	•	実際の代表流量 Q_1,Q_2,Q_3
% 	•	全 run が静水開始条件を満たした, もしくは除外 run があればその扱い
% 図は, あっても 1枚までです. Appendix に回しても成立します.
% 率直に言うと, ここは本文 1 段落 + 小さな Table でも足ります.

\subsection{Inflow flow regimes}
\label{sec:results_inflow}
% ここでは, inflow のレジームをまず単独で整理します.

% 順番としては
% 	1.	代表ベクトル図 / streamline
% 	2.	primary metrics の h/a 依存
% 	3.	流量依存の整理

% が自然です.

% 出したいもの
% 	•	代表水深帯 B1--B4 の平均ベクトル図 or streamline 図
% 	•	E, \phi_{\mathrm{lv}}, I_{\mathrm{asym}}, I_{\mathrm{circ}} の h/a に対する変化
% 	•	必要なら low / medium / high の比較

% この小節で主張すること
% 	•	inflow では偏向ジェットと二循環構造がどう現れるか
% 	•	h/a \approx 1 付近で何が変わるか
% 	•	流量を上げるとその傾向がどう変化するか

% ここでは I_{\mathrm{unst}} は脇役でよいです.
% 主役は
% 	•	I_{\mathrm{asym}}
% 	•	I_{\mathrm{circ}}
% 	•	\phi_{\mathrm{lv}}
% です.

\subsection{Outflow flow regimes}
\label{sec:results_outflow}
% ここは inflow と対称な構成に見せつつ, 非定常性を前景化します.

% 出したいもの
% 	•	代表水深帯 B1--B4 の平均ベクトル図
% 	•	可能なら代表的な瞬時場 1〜2 枚
% 	•	E, \phi_{\mathrm{lv}}, I_{\mathrm{asym}}, I_{\mathrm{circ}} の h/a 依存
% 	•	I_{\mathrm{unst}}(B_k) の整理

% この小節で主張すること
% 	•	outflow では basin 全体を占める単一循環がどう現れるか
% 	•	inflow より平均場だけでは捉えにくい変動性が強いこと
% 	•	h/a によって非定常性がどう変わるか

% ここでは逆に, I_{\mathrm{unst}} を前面に出してよいです.
% この論文の「平均場だけで終わらない」部分がここです.

\subsection{Comparative regime transition and mode asymmetry}
\label{sec:results_comparison}
% ここが Results の核です.
% inflow と outflow を並べるのは, 最後にまとめてやる方が強いです.

% 出したいもの
% 	•	inflow / outflow を同じ軸上で比較した h/a 依存曲線
% 	•	regime map
% 	•	代表図の対比

% この小節で主張すること
% 	•	同じ port を共有しても inflow / outflow は非対称である
% 	•	レジーム遷移は h/a に強く支配される
% 	•	とくに h/a \approx 1 近傍が転換帯である
% 	•	outflow では非定常性が相対的に強い

% この小節があると, Introduction の問い 3 つに真正面から答えられます.
